name: pr-labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "All tests passed!"
